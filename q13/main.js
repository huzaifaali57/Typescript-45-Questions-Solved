var fav_transport = ["Lamborghinis", "Porsches", "Bentleys", "Private Jets", "Yachts"];
for (var i = 0; i < fav_transport.length; i++) {
    console.log("I would love to own ".concat(fav_transport[i]));
}
